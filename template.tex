\documentclass[[-- options.style --][#- if options.astrosymb -#]
,astrosymb
[#- endif -#][#- if options.tighten -#]
,tighten
[#- endif -#][#- if options.times -#]
,times
[#- endif -#][#- if options.linenumbers -#]
,linenumbers
[#- endif -#][#- if options.trackchanges -#]
,trackchanges
[#- endif -#][#- if options.longauthor -#]
,longauthor
[#- endif -#][#- if options.twocolappendix -#]
,twocolappendix
[#- endif -#][#- if options.anonymous -#]
,anonymous
[#- endif -#]]{aastex631}
[- IMPORTS -]
[# if options.received #]\received{[- options.received -]}[# endif #]
[# if options.revised #]\revised{[- options.revised -]}[# endif #]
[# if options.accepted #]\accepted{[- options.accepted -]}[# endif #]
[# if options.submitjournal #]\received{[- options.submitjournal -]}[# endif #]
[# if options.shorttitle #]\shorttitle{[- options.shorttitle -]}[# endif #]
[# if options.shortauthors #]\shortauthors{[- options.shortauthors -]}[# endif #]
[# if options.watermark #]\watermark{[- options.watermark -]}[# endif #]
[# if parts.preamble #][- parts.preamble -][# endif #]
\begin{document}

\title{[-doc.title-]}

[# if parts.authors #]
[- parts.authors -]
[# else #]
[# for author in doc.authors #]
[# if author.corresponding #]
\correspondingauthor{[- author.name -]}
[# if author.email #]
\email{[- author.email -]}
[# endif #]
[# endif #]
[# endfor #]

[# for author in doc.authors #]
\author[# if author.orcid #][[-- author.orcid --]][# endif #]{[-- author.name --]}
[# for affil in author.affiliations #]
\affiliation{[-- affil.value --]}
[# endfor #]
[# for affil in author.altaffiliations #]
\altaffiliation{[-- affil.value --]}
[# endfor #]

[# endfor #]
[# endif #]
[# if parts.abstract #]
\begin{abstract}
[-parts.abstract-]
\end{abstract}
[# endif #]

%% Keywords should appear after the \end{abstract} command. 
%% The AAS Journals now uses Unified Astronomy Thesaurus concepts:
%% https://astrothesaurus.org
%% You will be asked to selected these concepts during the submission process
%% but this old "keyword" functionality is maintained in case authors want
%% to include these concepts in their preprints.
\keywords{Classical Novae (251) --- Ultraviolet astronomy(1736) --- History of astronomy(1868) --- Interdisciplinary astronomy(804)}

%% From the front matter, we move on to the body of the paper.
%% Sections are demarcated by \section and \subsection, respectively.
%% Observe the use of the LaTeX \label
%% command after the \subsection to give a symbolic KEY to the
%% subsection for cross-referencing in a \ref command.
%% You can use LaTeX's \ref and \label commands to keep track of
%% cross-references to sections, equations, tables, and figures.
%% That way, if you change the order of any elements, LaTeX will
%% automatically renumber them.
%%
%% We recommend that authors also use the natbib \citep
%% and \citet commands to identify citations.  The citations are
%% tied to the reference list via symbolic KEYs. The KEY corresponds
%% to the KEY in the \bibitem in the reference list below. 

[-CONTENT-]

[# if part.acknowledgments #]
\begin{acknowledgments}
[- part.acknowledgments -]
\end{acknowledgments}
[# endif #]

[# if part.facilities #]
\facilities{[- part.facilities -]}
[# endif #]

[# if part.software #]
\software{[- part.software -]}
[# endif #]

[# if part.appendix #]
\appendix
[- part.appendix -]
[# endif #]

[# if doc.bibliography #]
\bibliography{[- doc.bibliography -]}{}
\bibliographystyle{aasjournal}
[# endif #]

\end{document}
